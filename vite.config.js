import { defineConfig } from 'vite'

export default defineConfig({
  // This ensures paths point to https://peppy8651.github.io/RamirosWeb/ 
  // instead of the root domain.
  base: '/RamirosWeb/', 
})
